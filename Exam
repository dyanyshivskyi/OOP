class MyClass:
    """
    Цей клас є прикладом того, як додавати документаційний рядок до класу в Python.
    Він містить кілька атрибутів і методів для демонстрації основних принципів об'єктно-орієнтованого програмування.
    
    Атрибути:
        name (str): Ім'я об'єкта.
        value (int): Числове значення, яке зберігає об'єкт.
    
    Методи:
        display_info(): Виводить інформацію про об'єкт.
    """
    
    def __init__(self, name, value):
        self.name = name
        self.value = value

    def display_info(self):
        print(f"Ім'я: {self.name}, Значення: {self.value}")

# Створення об'єкта класу
obj = MyClass("Example", 42)

# Виведення опису класу
print(MyClass.__doc__)

# Виклик методу для виведення інформації про об'єкт
obj.display_info()
